# Frontend environment variables (Create React App)
# Set these in .env (managed by orchestrator/deployment). Do not commit secrets.

# Base URL for the backend API (FastAPI). Example: http://localhost:3001
REACT_APP_API_BASE_URL=
